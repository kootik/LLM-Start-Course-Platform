# Домашнее задание 1: Практическое применение

**Срок сдачи:** 7 дней
**Формат:** Jupyter Notebook (`.ipynb`) для задач 1.1-1.3 и экспортированный JSON для задачи 1.4.

---

## Задача 1.1: Линейная регрессия с нуля (NumPy)
**Цель:** Понять математику "под капотом" метода `fit()`.

1.  Загрузите датасет **Boston Housing** (или аналогичный, например, California Housing, так как Boston исключен из новых версий sklearn).
2.  Реализуйте класс `LinearRegression` используя только `numpy`. Вам нужно решить нормальное уравнение:
    $$w = (X^T X)^{-1} X^T y$$
    *Не забудьте добавить столбец единиц для смещения (bias)!*
3.  Сравните свои веса и метрику MSE с реализацией из `sklearn.linear_model.LinearRegression`.

---

## Задача 1.2: Классификация ирисов (Decision Trees)
**Цель:** Изучить влияние гиперпараметров на переобучение.

1.  Загрузите **Iris dataset**.
2.  Обучите `DecisionTreeClassifier` с разной глубиной (`max_depth` от 1 до 10).
3.  Постройте график зависимости **Accuracy** от **глубины дерева**.
4.  Визуализируйте лучшее дерево с помощью `plot_tree`.
    *Ожидаемый вывод:* На какой глубине наступает "плато" точности или начинается переобучение?

---

## Задача 1.3: Нейросеть MLP для MNIST
**Цель:** Написать первую нейросеть на PyTorch.

1.  Загрузите датасет рукописных цифр **MNIST**.
2.  Создайте класс `MLP` (Multi-Layer Perceptron):
    * Вход: $28 \times 28 = 784$.
    * Скрытые слои: 128 (ReLU, Dropout), 64 (ReLU, BatchNorm).
    * Выход: 10 классов.
3.  Напишите цикл обучения (Training Loop) на 5-10 эпох.
4.  Выведите точность (Accuracy) на тестовом наборе. Ожидается $>95\%$.

---

## Задача 1.4: n8n Workflow (Творческое задание)
**Цель:** Закрепить навыки работы с low-code.

1.  Модифицируйте workflow из практического урока.
2.  Измените промпт (user content) так, чтобы модель принимала на вход **тему** и выдавала **3 идеи для стартапа** по этой теме.
3.  *Опционально:* Добавьте второй HTTP Request, который берет лучшую идею и пишет для нее слоган.
4.  Экспортируйте workflow в JSON файл (`File` -> `Export Workflow`).